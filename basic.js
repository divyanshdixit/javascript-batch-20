var x ; // declaration => undefined
x = 'string'; // define

var X = 10.1;
var y = true; 

// var 1s = 10 (not allowed)

// primitive (mutable)
String, number, Boolean, undefined

const t = 1; // 1000 reference =>
t = 20; // 1001

// non-primitive (unmuntable)
object 
null, object, array => specila kind of object 

const obj1 = { // 2000
    name: 'divyansh',
    city: 'kanpur'
}
obj1 = { // 2001
    name: 'divyansh',
    city: 'kanpur'
}
obj1.name = 'new name'
console.log(obj1);


// console.log()
// alert()
// document.body()
// DOM => getDocumentById('demo').innerHTML = ''

